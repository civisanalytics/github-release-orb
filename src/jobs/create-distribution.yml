description: |
  Publish a new GitHub Release for a Python repository with a distribution included.

executor: python

steps:
  - checkout
  - run-setup-distribution
  - persist_to_workspace:
      root: .
      paths:
        - ./*
